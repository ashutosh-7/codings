#include <iostream>
#include <string>
using namespace std;

int main() {
    string s,s2,x;x="";
	cin>>s;
    cin>>s2;
    int a=s.size();
    int b=s2.size();
    cout<<a<<" "<<b<<endl;
    cout<<s+s2<<endl;
    cout<<s2[0];
    for(int i=1;i<a;i++)
    {
        cout<<s[i];
    }
    cout<<" "<<s[0];
    for(int i=1;i<b;i++)
    {
        cout<<s2[i];
    }
    
  
    return 0;
}
